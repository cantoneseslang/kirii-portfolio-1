# 管理者権限設定ガイド

## 問題：管理者権限が反映されない

SupabaseのSQLスクリプトで管理者権限を付与しても反映されない場合、以下の手順で確実に設定を行ってください。

## ステップ1：Supabaseダッシュボードにアクセス

1. [Supabase](https://app.supabase.com/)にログインします
2. プロジェクト「kirii-port」を選択します

## ステップ2：SQLエディタでスクリプトを実行

1. 左側のメニューから「SQL Editor」をクリックします
2. 「New query」ボタンをクリックして新しいクエリを作成します
3. `admin-user-permissions.sql` ファイルの内容をコピーしてエディタに貼り付けます
4. 「Run」ボタンをクリックしてSQLを実行します
5. 実行結果に表示されるSELECTクエリの結果を確認します
   - `user_meta_is_admin` と `profile_is_admin` の両方が `true` になっていることを確認

## ステップ3：テーブルエディタで直接確認

もしSQLでの更新が反映されない場合は、テーブルエディタで直接確認・編集できます：

1. 左側のメニューから「Table Editor」をクリックします
2. `profiles` テーブルを選択します
3. 検索欄に「hiroki.sakon」と入力して検索します
4. 該当ユーザーの行を見つけて `is_admin` 列を確認します
5. `is_admin` の値が `false` または `null` の場合、編集アイコンをクリックして `true` に変更します

## ステップ4：Auth管理画面で確認

Auth側でもメタデータを確認できます：

1. 左側のメニューから「Authentication」→「Users」をクリックします
2. hiroki.sakonのユーザーを探します
3. 「User Metadata」列を確認し、`"is_admin": true` が含まれていることを確認
4. 含まれていない場合、「Edit User」から「User Metadata」に `{"is_admin": true}` を追加します

## ステップ5：アプリケーションを再起動（オプション）

変更が反映されない場合はVercelでアプリケーションを再デプロイしてみてください：

```bash
vercel --prod
```

## ステップ6：ブラウザキャッシュのクリア

最後に、ブラウザのキャッシュをクリアして再度ログインしてみてください：

1. ブラウザのプライベートウィンドウ/シークレットモードで開く
2. または、ブラウザの開発者ツールを開き、「Application」→「Storage」→「Clear site data」
3. 再度ログインして、Admin機能にアクセスできるか確認

## トラブルシューティング

もし上記の手順を試しても問題が解決しない場合：

1. app/admin/page.tsx の checkIsAdmin 関数の実装を確認
2. Supabaseコンソールで正しいプロジェクトにアクセスしているか確認
3. デプロイされているバージョンが最新かどうか確認
