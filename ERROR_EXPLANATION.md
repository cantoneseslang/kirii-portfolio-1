# 「Database error querying schema」エラーの説明と根本原因

## エラー発生箇所

```javascript
// components/login-form.tsx (31行目)
const { data, error } = await supabase.auth.signInWithPassword({
  email,
  password,
})
```

## エラーの意味

「Database error querying schema」（データベースのスキーマ取得中にエラーが発生しました）というエラーは、Supabaseの認証システムが内部的にデータベースのスキーマ情報を取得しようとしたときに発生しています。

## 技術的な説明

このエラーが発生する根本的な原因は以下の通りです：

1. **スキーマアクセス権限の問題**:
   - Supabaseの認証システムは、ログイン時にいくつかのテーブルのスキーマ情報を確認します
   - アプリケーションで使用している認証キー（ANON_KEY）には、これらのスキーマ情報を読み取る十分な権限がありません

2. **Row Level Security (RLS)の設定不備**:
   - RLSポリシーが正しく設定されていないため、auth.usersテーブルなど認証関連のテーブルにアクセスできません
   - これにより、認証プロセスがスキーマ情報を取得できずに失敗します

3. **プロファイル情報の不整合**:
   - ユーザーはauth.usersテーブルには存在するが、profilesテーブルにデータがない
   - または、profilesテーブルの構造が認証システムの期待する形式と一致していない

## Supabaseの二層構造認証システム

Supabaseでは、認証は複雑な二層構造で実装されています：

1. **auth.users テーブル** - 実際の認証情報（メールアドレスとパスワード）を保存
2. **profiles テーブル** - ユーザープロファイル情報を保存

認証プロセスでは、この両方のテーブルとその関連するスキーマ情報にアクセスする必要があります。どちらかのテーブルに問題があると、このようなエラーが発生します。

## 解決方法

このエラーを解決するには、Supabaseダッシュボードで「postgres role」権限を使用して、提供したSQLスクリプト（postgres-role-rls-fix.sql）を実行する必要があります。このスクリプトは：

1. RLSを一時的に無効化する、または適切なポリシーを設定する
2. 必要なプロファイルデータが確実に存在するようにする
3. テーブル構造と権限の問題を修正する

これにより、認証システムが必要なスキーマ情報にアクセスできるようになり、ログインが正常に機能するようになります。
