# エクセル表のユーザーをSupabaseに登録する解決策（更新版）

## 認証エラーの解決

「Database error querying schema」エラーが発生した場合、これはSQLでユーザーを追加する際に必要なRow Level Security (RLS)ポリシーの設定が不足していることが原因です。

## 正しい実装方法

新しい`working-user-import.sql`ファイルを用意しました。このファイルは：

1. **プロフィールテーブルの構造を確認/作成**
2. **必要なセキュリティポリシー（RLS）を設定**
3. **ユーザーデータを追加**

これらの手順はすべて、動作確認済みの`create-user-with-working-password.sql`ファイルに基づいています。

## 実行手順

1. [Supabaseのダッシュボード](https://app.supabase.io/)にログイン
2. プロジェクト「mnshbcvrrzlumfomniim」を選択
3. 左側メニューから「SQL Editor」を選択
4. 「+ New Query」ボタンをクリック
5. `working-user-import.sql`の内容をコピーして貼り付け
6. 「Run」ボタンをクリックして実行

SQLファイルには2ユーザー分が含まれていますが、同じパターンで残りのユーザーも追加できます。

## RLSポリシーの重要性

Supabaseでは、Row Level Security (RLS)が認証システムと密接に連携しています。これらのポリシーがないと、ユーザーはログインできても、必要なデータにアクセスできない場合があります。

SQLスクリプトで設定している重要なRLSポリシー：

1. **自分のプロフィールの表示権限**
2. **自分のプロフィールの更新権限**
3. **管理者の全プロフィール表示権限**

## 注意点

1. **メールアドレスの重複**: 複数のユーザーが同じメールアドレス（info@kirii.com.hk）を使用している場合、info1@kirii.com.hk、info2@kirii.com.hkなどのように変更してください。

2. **usernameカラムの一意性**: `username`カラムはUNIQUE制約があるため、各ユーザーに一意の値を設定する必要があります。

3. **既存ユーザーの扱い**: SQLには`ON CONFLICT (...)` 句があるため、既存のユーザーは上書きされず、エラーも発生しません。
