# Supabaseの認証システムについての説明

## 現在の状況と疑問点の解消

1. **hiroki.sakon@kirii.com.hk でログインできる理由**：
   - このユーザーは既にSupabaseの認証システムに登録されています
   - スクリーンショットでは認証テーブルに表示されているため

2. **テーブルにデータがなくてもログインできる理由**：
   - Supabaseでは認証データは特別な「auth」スキーマのテーブルに保存されています
   - 通常のテーブルエディタでは直接見えないか、限定的な閲覧しかできません
   - 認証テーブル（`auth.users`）は特別な権限でのみアクセス可能です

## Supabaseの認証システムの仕組み

Supabaseでは、ユーザー認証情報とアプリケーションデータが異なるレベルで管理されています：

1. **認証レイヤー（auth.users）**：
   - ユーザーのメールアドレス、パスワード、認証情報を保存
   - ログイン処理、パスワード検証を担当
   - Supabase管理画面の「Authentication」→「Users」で確認可能

2. **アプリケーションレイヤー（profiles）**：
   - ユーザーの追加情報（名前、部門、役職など）
   - アプリケーション固有のデータ
   - 通常のデータベーステーブルとして利用可能

これらは内部的にユーザーIDで関連付けられています。

## ユーザーデータをインポートする際の注意点

1. **認証データとプロフィールデータの両方を追加する必要がある**：
   - `auth.users`テーブルにユーザーの認証情報を挿入
   - `profiles`テーブルにユーザーのプロフィール情報を挿入
   - 同じIDで両方のテーブルをリンク

2. **SQLの直接実行が必要**：
   - CSVインポートでは`auth.users`テーブルにアクセスできないため、SQLで直接挿入する必要がある
   - 提供した`insert-users-final.sql`スクリプトがこれを行います

## Supabaseの管理画面でのデータ確認方法

1. **ユーザー認証データの確認**：
   - 左側メニューから「Authentication」→「Users」を選択
   - ここにログイン可能なすべてのユーザーが表示されます

2. **プロフィールデータの確認**：
   - 左側メニューから「Table Editor」→「profiles」を選択
   - ユーザーの追加情報がここに表示されます

## SQLインジェクションの実行方法

1. Supabaseダッシュボードにログイン
2. 左側メニューから「SQL Editor」を選択
3. 「+ New Query」ボタンをクリック
4. `insert-users-final.sql`の内容をコピーして貼り付け
5. 「Run」ボタンをクリックして実行

これにより、認証テーブルとプロフィールテーブルの両方に正しくデータが追加され、ユーザーはログインできるようになります。
