# Supabaseでのログイン認証とプロフィールテーブルの関係

## 表示されている画面の説明

1. **最初のスクリーンショット（プロフィールテーブル）**:
   - これは`profiles`テーブルの表示画面です
   - ここにはユーザー名、名前、部門などの情報が保存されています
   - **重要**: ここには認証情報（メールアドレスとパスワード）は表示されていません

2. **二番目のスクリーンショット（ログイン画面）**:
   - これはユーザーがアクセスするログインフォームです
   - メールアドレスとパスワードを入力する欄があります

## Supabaseの二層構造認証システム

Supabaseは認証データとプロフィールデータを**別々のテーブル**で管理しています：

### 1. 認証テーブル（auth.users）

- メールアドレスとパスワードはこちらに保存
- 通常のデータベースビューには表示されない
- Auth管理画面からのみアクセス可能
- ログイン処理はこのテーブルを参照

### 2. プロフィールテーブル（profiles）

- ユーザー情報（名前、役職など）を保存
- 通常のデータベースビューで表示される
- ログイン認証には使用されない

## ログインの仕組み

ユーザーがログインするとき：

1. 入力されたメールアドレスとパスワードは、コードの`supabase.auth.signInWithPassword()`関数によって処理されます
2. この関数は**`auth.users`テーブル**を内部的に検索します（プロフィールテーブルではない）
3. メールアドレスとパスワードが一致すると、ログイン成功

```javascript
// login-form.tsxファイルより抜粋
const handleSignIn = async (e: React.FormEvent) => {
  // ...
  const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password,
  })
  // ...
}
```

## 提供したSQLスクリプトの説明

`insert-users-final.sql`ファイルは、ユーザーごとに**両方のテーブル**にデータを追加します：

1. **認証データをauth.usersテーブルに追加**:
   ```sql
   INSERT INTO auth.users (
     /* フィールド */
   ) VALUES (
     /* 値... */
     'hiroki.sakon@kirii.com.hk',  -- メールアドレス
     crypt('sakon0201', gen_salt('bf')),  -- パスワード
     /* その他の値... */
   );
   ```

2. **プロフィールデータをprofilesテーブルに追加**:
   ```sql
   INSERT INTO profiles (
     /* フィールド */
   ) SELECT 
     /* 値... */
   FROM auth.users
   WHERE email = 'hiroki.sakon@kirii.com.hk';
   ```

このSQLファイルを実行すると、両方のテーブルにデータが入り、ユーザーはログインできるようになります。

## まとめ

1. プロフィールテーブルには`email`列がなくても問題ありません
2. 認証情報は別の特別なテーブル（`auth.users`）に保存されています
3. ログイン処理はこの特別なテーブルを参照しています
4. 両方のテーブルにデータを入れることで、ログインとプロフィール表示の両方が機能します
